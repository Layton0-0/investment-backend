# QA 실행 리포트 (2026-02-05)

**실행 일시**: 2026-02-05  
**대상**: 백엔드(8084) + 프론트(5173) 연동 환경  
**목적**: 라이브 스모크, 백엔드 테스트, 프론트 테스트 자동 실행 및 결과 정리

---

## 1. 라이브 스모크 테스트

| 항목 | URL | 결과 | 비고 |
|------|-----|------|------|
| 백엔드 Health | `GET http://localhost:8084/actuator/health` | **PASS** | 200, status UP |
| 프론트 진입 | `GET http://localhost:5173/` | **PASS** | 200, title "Investment Choi" |
| Auth 엔드포인트 | `POST http://localhost:8084/api/v1/auth/login` | **PASS** | 302 (정상 응답·리다이렉트) |

**결론**: 서버 기동 및 공개 엔드포인트 접근 정상.

---

## 2. 백엔드 테스트 (Gradle)

- **명령**: `investment-backend\scripts\run-tests.ps1`
- **결과**: **259 tests completed, 30 failed**
- **실패 요약**:
  - `InvestmentApplicationTests > contextLoads()`: `DataSourceProperties` / DataSource 빈 생성 실패 (테스트 환경 DB 설정 이슈로 추정)
  - 기타 29건: Spring Context 미기동으로 인한 `IllegalStateException` / `NoSuchBeanDefinitionException` (위 원인 연쇄)
- **권장**: 테스트 전용 프로필·H2 또는 Testcontainers 등으로 테스트 시 DataSource 설정 보완 후 재실행

---

## 3. 프론트 테스트 (Vitest)

- **명령**: `investment-front`: `npm test -- --run`
- **결과**: **6 test files, 24 tests, 전부 통과**
- **포함**: `inputValidation`, `http`, `secureUrl`, `useDashboardData`, `LoginPage`, `AppRoutes`

---

## 4. 종합

| 구분 | 결과 | 비고 |
|------|------|------|
| 라이브 스모크 | **PASS** | 백엔드·프론트·Auth 엔드포인트 정상 응답 |
| 프론트 단위 테스트 | **PASS** | 24/24 통과 |
| 백엔드 단위/통합 테스트 | **일부 실패** | 229 통과, 30 실패 (Context/DataSource 설정 이슈) |

**다음 권장**: 백엔드 테스트 실패 원인(테스트용 DB/프로필) 해결 후 전체 QA 재실행.
